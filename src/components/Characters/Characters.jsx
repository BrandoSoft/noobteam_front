import React from 'react';
import { LoginForm } from "../LoginForm/LoginForm";

export const Characters = () => {
    return (
        <div>
            <LoginForm/>
            <AddCharacter/>
        </div>
    );
};
